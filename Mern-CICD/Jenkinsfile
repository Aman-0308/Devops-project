pipeline {
    agent any  // Defines that the pipeline will run on any available agent
    stages {
        stage("SCM checkout") {
            steps {
                echo "Hello Aman"  // Prints a message to the Jenkins console
            }
        }
    }
}
